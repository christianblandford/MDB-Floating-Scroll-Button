# MDB-Floating-Scroll-Button
A floating button that appears only when scrolled past a certain point. Clicking the button returns to the top of the page.
Must be used with Jquery, MDBootstrap, and Bootstrap.
